

fix feedback
add the other sts library, just way better

make python datastructs

rework the async setting
make the async writing for every command, without explicit commands with a -1 servo id or smth





